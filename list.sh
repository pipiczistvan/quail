#!/bin/bash

ls $TRAVIS_BUILD_DIR -la
ls $TRAVIS_BUILD_DIR/client -la
ls $TRAVIS_BUILD_DIR/client/android -la
ls $TRAVIS_BUILD_DIR/client/android/app -la
ls $TRAVIS_BUILD_DIR/client/android/app/build -la
ls $TRAVIS_BUILD_DIR/client/android/app/build/outputs -la
ls $TRAVIS_BUILD_DIR/client/android/app/build/outputs/apk -la
ls $TRAVIS_BUILD_DIR/client/android/app/build/outputs/apk/release/ -la
